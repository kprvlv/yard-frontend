language: node_js
node_js:
  - "node"
  cache:
  yarn: true
  directories:
    - node_modules
install:
  - yarn install
  - yarn flow-typed:install
script:
  - yarn lint
  - yarn flow
deploy:
  provider: script
  script:
    - yarn deploy
  on:
    branch: master
